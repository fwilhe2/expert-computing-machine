contents:
  keyring:
    - https://packages.wolfi.dev/os/wolfi-signing.rsa.pub
  repositories:
    - http://localhost:8000
  packages:
    - bash
    - coreutils
    - systemd-init
    - libsystemd

archs:
- x86_64
