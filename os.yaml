contents:
  keyring:
    - https://packages.wolfi.dev/os/wolfi-signing.rsa.pub
  repositories:
    - http://localhost:8000
  packages:
    - bash
    - dash-binsh
    - coreutils
    - systemd-init
    - libsystemd
    - mount

archs:
- x86_64
